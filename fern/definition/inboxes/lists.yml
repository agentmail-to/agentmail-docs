# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

imports:
  global: ../__package__.yml
  inboxes: __package__.yml
  lists: ../lists.yml

service:
  url: Http
  base-path: /inboxes/{inbox_id}/lists/{direction}/{type}
  path-parameters:
    inbox_id: inboxes.InboxId
    direction: lists.Direction
    type: lists.ListType
  auth: true

  endpoints:
    list:
      method: GET
      path: ""
      display-name: List Entries
      docs: |
        Use the AgentMail API to list entries within a specific inbox. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail inboxes:lists list --inbox-id <inbox_id> --direction <direction> --type <type>
        ```
      request:
        name: InboxListListEntriesRequest
        query-parameters:
          limit: optional<global.Limit>
          page_token: optional<global.PageToken>
      response: lists.PodListListEntriesResponse

    get:
      method: GET
      path: /{entry}
      display-name: Get List Entry
      docs: |
        Use the AgentMail API to get a list entry within a specific inbox. Review authentication, parameters, responses, and errors. See the complete endpoint schema.

        **CLI:**
        ```bash
        agentmail inboxes:lists get --inbox-id <inbox_id> --direction <direction> --type <type> --entry <entry>
        ```
      path-parameters:
        entry:
          type: string
          docs: Email address or domain.
      response: lists.PodListEntry
      errors:
        - global.NotFoundError

    create:
      method: POST
      path: ""
      display-name: Create List Entry
      docs: |
        Use the AgentMail API to create a list entry within a specific inbox. Review authentication, parameters, responses, and errors. Review the endpoint schema.

        **CLI:**
        ```bash
        agentmail inboxes:lists create --inbox-id <inbox_id> --direction <direction> --type <type> --entry user@example.com
        ```
      request: lists.CreateListEntryRequest
      response: lists.PodListEntry
      errors:
        - global.ValidationError

    delete:
      method: DELETE
      path: /{entry}
      display-name: Delete List Entry
      docs: |
        Use the AgentMail API to delete a list entry within a specific inbox. Review authentication, parameters, responses, and errors. Review the endpoint schema.

        **CLI:**
        ```bash
        agentmail inboxes:lists delete --inbox-id <inbox_id> --direction <direction> --type <type> --entry <entry>
        ```
      path-parameters:
        entry:
          type: string
          docs: Email address or domain.
      errors:
        - global.NotFoundError
