# yaml-language-server: $schema=https://raw.githubusercontent.com/fern-api/fern/main/fern.schema.json

imports:
  global: ../__package__.yml
  pods: __package__.yml
  domains: ../domains.yml

service:
  url: Http
  base-path: /pods/{pod_id}/domains
  path-parameters:
    pod_id: pods.PodId
  auth: true

  endpoints:
    list:
      method: GET
      path: ""
      display-name: List Domains
      docs: |
        Use the AgentMail API to list domains within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains list --pod-id <pod_id>
        ```
      request:
        name: ListDomainsRequest
        query-parameters:
          limit: optional<global.Limit>
          page_token: optional<global.PageToken>
          ascending: optional<global.Ascending>
      response: domains.ListDomainsResponse
      errors:
        - global.NotFoundError

    get:
      method: GET
      path: /{domain_id}
      display-name: Get Domain
      docs: |
        Use the AgentMail API to get a domain within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains get --pod-id <pod_id> --domain-id <domain_id>
        ```
      path-parameters:
        domain_id: domains.DomainId
      response: domains.Domain
      errors:
        - global.NotFoundError

    getZoneFile:
      method: GET
      path: /{domain_id}/zone-file
      display-name: Get Zone File
      docs: |
        Use the AgentMail API to get a zone file within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains get-zone-file --pod-id <pod_id> --domain-id <domain_id>
        ```
      path-parameters:
        domain_id: domains.DomainId
      response: file
      errors:
        - global.NotFoundError

    create:
      method: POST
      path: ""
      display-name: Create Domain
      docs: |
        Use the AgentMail API to create a domain within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains create --pod-id <pod_id> --domain example.com
        ```
      request: domains.CreateDomainRequest
      response: domains.Domain
      errors:
        - global.ValidationError

    update:
      method: PATCH
      path: /{domain_id}
      display-name: Update Domain
      docs: |
        Use the AgentMail API to update a domain within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains update --pod-id <pod_id> --domain-id <domain_id>
        ```
      path-parameters:
        domain_id: domains.DomainId
      request: domains.UpdateDomainRequest
      response: domains.Domain
      errors:
        - global.NotFoundError

    delete:
      method: DELETE
      path: /{domain_id}
      display-name: Delete Domain
      docs: |
        Use the AgentMail API to delete a domain within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains delete --pod-id <pod_id> --domain-id <domain_id>
        ```
      path-parameters:
        domain_id: domains.DomainId
      errors:
        - global.NotFoundError

    verify:
      method: POST
      path: /{domain_id}/verify
      display-name: Verify Domain
      docs: |
        Use the AgentMail API to verify a domain within a specific Pod. Review authentication, request parameters, response fields, error behavior, and usage details.

        **CLI:**
        ```bash
        agentmail pods:domains verify --pod-id <pod_id> --domain-id <domain_id>
        ```
      path-parameters:
        domain_id: domains.DomainId
      errors:
        - global.NotFoundError
