---
title: Quickstart
subtitle: Getting Started with AgentMail
slug: quickstart
description: Quick start guide for installing and setting up AgentMail in your project
---

## SDK's

We currently support Python and TypeScript/Node.js. If you would like us to support another language, please [reach out to us](mailto:support@agentmail.to).

<Cards>
  <Card
    title="Python"
    icon="fa-brands fa-python"
    href="https://github.com/agentmail-to/agentmail-python"
  />
  <Card
    title="Node/TS"
    icon={<img src="./ts.svg" style={{ height: 24 }} />}
    href="https://github.com/agentmail-to/agentmail-node"
  />
</Cards>

## Quick Install

Install AgentMail using your preferred package manager:

<CodeBlocks>
```bash title="Python"
pip install agentmail
```

```bash title="Node"
npm install agentmail
```

</CodeBlocks>

## Prerequisites

- Python 3.8 or higher
- An AgentMail API key ([Request your API key](https://form.typeform.com/to/m0kKD0jH))

## Verify Installation

Test your installation:

```python
from agentmail import AgentMail

# Should print the version number
print(AgentMail.__version__)
```

## Basic Usage

Here's how to initialize the client and create your first inbox:

<CodeBlocks>
```python title="Python"
from agentmail import AgentMail

# Initialize the client

client = AgentMail(api_key="YOUR_API_KEY")

# Create an inbox

inbox = client.inboxes.create(domain="yourdomain.com")

````

```typescript title="TypeScript/Node.js"
import { AgentMailClient } from "agentmail";

// Initialize the client
const client = new AgentMailClient({ apiKey: "YOUR_API_KEY" });

// Create an inbox
await client.inboxes.create({
  domain: "yourdomain.com",
});
```

</CodeBlocks>

<Tip>
  💡 We recommend using environment variables for your API key in production.
  Create a `.env` file and use python-dotenv to load it: ```bash
  AGENTMAIL_API_KEY=your_api_key_here ```
</Tip>

## Error Handling

Handle API errors gracefully with built-in error types:

<CodeBlocks>
```python title="Python"
from agentmail.core.api_error import ApiError

try:
    client.inboxes.create(domain="yourdomain.com")
except ApiError as e:
    print(f"Error {e.status_code}: {e.body}")

```

```typescript title="TypeScript/Node.js"
import { AgentMailError } from "agentmail";

try {
  await client.inboxes.create({ domain: "yourdomain.com" });
} catch (err) {
  if (err instanceof AgentMailError) {
    console.log(`Error ${err.statusCode}: ${err.message}, Message: ${err.body}`);
  }
}
```

</CodeBlocks>

## Async Support

Python users can also use our async client for non-blocking operations:

```python title="Python Async"
import asyncio
from agentmail import AsyncAgentMail

async_client = AsyncAgentMail(api_key="YOUR_API_KEY")

async def main():
    await async_client.inboxes.create(domain="yourdomain.com")

asyncio.run(main())
```


## Environment Support

- **Python**: Python 3.8+
- **TypeScript/Node.js**: Node.js 18+, Vercel, Cloudflare Workers, Deno v1.25+, Bun 1.0+, React Native

## Next Steps

- [Quick Start Guide](./quickstart) - Build your first email agent
- [Using Agents](/documentation/how-to/using-agents) - Learn how to create AI agents with email capabilities
- [API Reference](/api-reference) - Explore the full API documentation
- [Shopper Agent Tutorial](/documentation/examples/shopper-agent) - Build an AI shopping assistant

## Need Help?
<Cards>
  <Card title="GitHub Issues" icon="fa-brands fa-github" href="https://github.com/agentmail-to" />
  <Card
    title="Discord Community"
    icon="fa-brands fa-discord"
    href="https://discord.gg/ZYN7f7KPjS"
  />
  <Card
    title="Email Support"
    icon="fa-solid fa-envelope"
    href="mailto:support@agentmail.to"
  />


</Cards>

<Note>
  Looking for a different language? Email us at [support@agentmail.to](mailto:support@agentmail.to) and we'll get you set up.
</Note>
````
