name: Sync to Stainless

on:
  push:
    branches:
      - main
    paths:
      - "fern/definition/**"
      - "fern/generators.yml"
      - "fern/api.yml"

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Fern
        run: npm install -g fern-api

      - name: Generate OpenAPI spec
        run: fern generate --group openapi --local
        env:
          FERN_TOKEN: ${{ secrets.FERN_TOKEN }}

      - name: Push to Stainless
        run: |
          SPEC_FILE=$(find openapi -name "openapi.*" -type f | head -1)
          jq -n --arg spec "$(cat "$SPEC_FILE")" \
            '{project: "agentmail", branch: "main", revision: {"openapi.json": {content: $spec}}}' \
            > /tmp/stainless-body.json
          curl -sf -X POST "https://api.stainless.com/v0/builds" \
            -H "Authorization: Bearer $STAINLESS_API_KEY" \
            -H "Content-Type: application/json" \
            -d @/tmp/stainless-body.json
        env:
          STAINLESS_API_KEY: ${{ secrets.STAINLESS_API_KEY }}
